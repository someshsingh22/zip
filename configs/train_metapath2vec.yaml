seed: 42
paths:
  out_dir: data/processed
model:
  embedding_dim: 300
  walk_length: 40
  context_size: 5
  walks_per_node: 10
  num_negative_samples: 5
loader:
  batch_size: 512
train:
  epochs: 3
  lr: 0.01
logging:
  log_every: 200
  wandb_project: reddit-gnn


